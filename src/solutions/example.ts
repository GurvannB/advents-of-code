import type {Solver} from '@core/types';

export default {
    name: '---',
    part1(input: string) {
        return "TO DO!";
    },
    part2(input: string) {
        return "TO DO!";
    }
} satisfies Solver;
